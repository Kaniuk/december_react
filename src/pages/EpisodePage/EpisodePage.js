import React from 'react';
import {useParams} from "react-router-dom";

const EpisodePage = () => {
    const {id} = useParams();

    return (
        <div>

        </div>
    );
};

export {EpisodePage};